<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quantumcomputersimulator">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/quantum"
        android:label="@string/app_name"
        android:roundIcon="@drawable/quantum_ic"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuantumSimulator"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".QuantumPuzzleActivity" />
        <activity android:name=".BlochSphereActivity" android:exported="false" />

    </application>

</manifest>
